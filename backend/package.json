{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "cd ./src && npx nodemon",
    "build": "npm install && rimraf ./build && npx tsc",
    "start": "node build/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.2.0",
    "rimraf": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
